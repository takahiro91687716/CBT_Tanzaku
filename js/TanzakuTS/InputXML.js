var InputXML = (function () {
    function InputXML() {
        this.xhr = new XMLHttpRequest();
    }
    return InputXML;
}());
function init() {
    xhr.open(method, fname, async);
    xhr.onreadystatechange = function () {
        if (xhr.readyState == 4) {
        }
    };
    xhr.send(null);
}
